<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css" />
    <title>Document</title>
    <style>
      body {
        box-sizing: border-box;
        top: 0;
        margin: 0;
      }
      nav {
        display: flex;
        justify-content: flex-end; /* pushes links to the right */
        background: #363333; /* optional */
        padding: 10px;
        font-family:sans-serif;
      }
    
      
      nav a {
        text-decoration: none;
        color: #ecebeb;
        margin-left: 25px;
        font-weight: bold;
      }
      nav a:hover{
        text-decoration: underline;
        transition: 0.5s ;
      }
      /* .outLink{
        background-color: rgb(240, 216, 186);
        width: 15%;
        height: 20%;
        display: flex;
        justify-content: center;
        align-items: center;
        float: right;
      }
      .outLink a{
        text-decoration: none;
        font-size: 20px;
        color: brown;
      } */
    </style>
  </head>
  <body>
    <nav>
      <a class="home" href="/">Home</a>
      <% if (!session.user) { %>
      <a href="/auth/register">Register</a>
      <a href="/auth/login">Login</a>
      <% } else { %>
      <a href="/posts/create">Create New Post</a>
      <a  href="/auth/logout"><%= session.user.userName %>: Logout</a>
      
      
      <% } %>
    </nav>
      
    <!-- <script>
      // function logoutLink(){

      //   let showLink=document.getElementById('logoutLink')
      //   if(showLink.style.display=== "none")
      //   showLink.style.display="block"
      // else{
      //   showLink.style.display="none";
      // }
      // }
    </script> -->
  </body>
</html>
